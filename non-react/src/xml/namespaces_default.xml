<?xml version="1.0"?>
<!-- This file is using DEFAULT NAMESPACES. In this case, the namespace URL is defined
     directly in an element. All the children of that element will belong to that namespace.
     in this way, a document does not need any prefixes. Less typing. -->
<toms_tables xmlns="http://tomsgreattables.com/ns">

    <!-- Furniture Table -->
    <table>
        <finish>Clearcoat</finish>
        <wood>Oak</wood>
        <dimensions>8 x 10 feet</dimensions>
    </table>

    <!-- HTML Table -->
    <!-- Here, we are using a SECOND default namespace in the <table> tag. The <table> tag and
         All of its children now belong to http://www.w3.org/TR/html4. After the closing <table>
         tag, we revert back to the original default namespace. -->
    <table xmlns="http://www.w3.org/TR/html4">
        <tr>
            <th>Dining Room</th>
            <th>Kitchen Tables</th>
            <th>Turned Tables</th>
        </tr>
        <tr>
            <td></td>
            <td></td>
            <td></td>
        </tr>
    </table>

    <!-- Since this element is OUTSIDE the above <table> element, it does not use that
         namespace. It will instead use the namespace of its parent, which is <toms_tables>
         and therefore, http://tomsgreattables.com. -->
    <order>1234567890</order>

    <!-- A problem with this method, is when I want to use ANOTHER HTML element, outside
           of the previous <table> tag. I need to use the "html" DEFAULT namespace AGAIN. -->
    <blockquote xmlns="http://www.w3.org/TR/html4">
        "I love Tom's Tables. They make my life worth living!" -Tom
    </blockquote>

</toms_tables>
